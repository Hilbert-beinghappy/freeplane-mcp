# Progress log

## 2026-08-01

- Confirmed user approval to implement the staged plan.
- Re-read the planning-with-files and OpenAI documentation skills.
- Confirmed the new APFS project path did not already exist.
- Initialized persistent plan, findings, and progress records.
- Current work: build a reproducible Freeplane menu/API inventory and GPT Pro consultation package.
- First package build exposed an incorrect user-guide category traversal; the generated deliverable was withheld and the parser/verification contract was tightened.
- ZIP verification then caught ExFAT `._*` metadata members; filtering was moved into the generator rather than relying on cleanup after packaging.
- Moved the Git repository to the user-selected ExFAT project volume; disabled Git file-mode tracking and removed only migration-created AppleDouble helper files.
- Rebuilt the package and passed manifest hash verification for 17 files, ZIP CRC/UTF-8 checks for 18 members, privacy checks, inventory thresholds, and real-example size checks.
- v0.0 deliverable created at `分析结果/Freeplane_MCP_GPTPro咨询包_20260801.zip`.
- The invalid intermediate directory and ZIP were moved to macOS Trash and remain recoverable.
- Current external gate: wait for the complete GPT Pro response before freezing or implementing v0.1.
- Received and read the complete implementation decision specification.
- Created the minimal npm monorepo with exact MCP SDK v2, Zod v4, TypeScript, and Node 22 constraints.
- Added the frozen response envelope, error registry, 12-tool name registry, and capability manifest schema.
- Implemented the Freeplane build, class, menu, built-in MCP, dependency, and Codex compatibility probes.
- Verified an actual isolated Codex app-server handshake and `freeplane_status` call without a model turn or global configuration change.
- Passed v0.0A with five tests; only status and capability inspection are registered.
- Current implementation gate: v0.0B transaction and real-time bridge qualification.
- Completed and qualified v0.0B with 22 passing checks, including real unsaved GUI detection, one-undo/redo equivalence, 100/100 rollback injections, stale-revision rejection, and unchanged fixture bytes.
- Committed v0.0B as `e9d67f1 feat(addon): qualify v0.0B bridge transactions` and pushed it to public repository `Hilbert-beinghappy/freeplane-mcp`.
- User created an active goal to finish v0.1, v0.2, v0.3, v0.4, v0.5, and v1.0 continuously; each version requires its own passing evidence, local commit, GitHub push, and remote verification.
- Current implementation gate: v0.1 read-only real-time MCP.
- v0.1 pre-edit baseline passed: five compiled TypeScript tests and the Java add-on self-test are green on the pinned environment.
- Added strict bounded v0.1 schemas for list/read/search/changes. Literal structured search is the only qualified search mode; scope, pagination, field, cursor, result, and wait limits are enforced. Protocol build and three focused tests pass.
- Implemented dependency-free, read-only `.mm` fallback with canonical allowlisted paths, no-follow regular-file reads, race/size/hash checks, strict UTF-8, bounded lexical XML parsing, and rejection of DTD/entities/XInclude/malformed input. Three focused fallback tests pass, including ordered duplicate Freeplane attributes and symlink/path denial.
- Added a strict loopback bridge client that validates discovery ownership/modes/schema/token/build/version/PID/expiry and rechecks instance identity through authenticated health before accepting bridge authority.
- Enriched the live bridge registry with active map, selection IDs, root ID, read-only/unsaved state, node-count estimate, and external file identity checks. The add-on compiles and its Java self-test passes.
- Connected status/capabilities/list/read/search/changes to the qualified bridge and explicit file fallback. The v0.1 server now exposes exactly six read-only tools, with bounded flat snapshots, scope-aware pagination, revision-bound page cursors, selection conflict checks, literal search, short polling, and full-resync errors.
- Updated both SDK and isolated Codex host contracts to the exact six-tool v0.1 surface. The full TypeScript suite now passes 10 tests, including real STDIO file degradation, pagination, duplicate attributes, search, and explicit no-journal failure.
- Hardened response-size/attribute limits, prevented a live read failure from silently substituting saved bytes after bridge identity was already established, and fixed null-cursor short polling so events cannot be skipped between polls. All 10 TypeScript tests remain green.
- Added qualification-only silent-reconciliation, bounded-journal-fill, and in-process bridge-restart hooks. Runtime hooks remain unavailable outside isolated qualification mode; normal registry shutdown now detaches listeners. The add-on self-test still passes.
- First full v0.1 live qualifier run failed only at in-process reconnect after 11 passing checks. The spawned process/profile were cleaned; the existing user Freeplane process remained untouched. Failed evidence is retained in `qualification/reports/v0.1-local.json` and will not be promoted to the capability manifest.
- Diagnosed the restart failure as Freeplane sandbox denial of `modifyThread`; confined the workaround to the qualification-only daemon pool and preserved normal shutdown behavior.
- The complete v0.1 qualifier now passes. Final metrics: status p95 2.89 ms, 5,000-node read p95 84.24 ms, GUI event visibility p95 902.73 ms, bridge reconnect 231.4 ms, and 50,000-event journal below 64 MiB. File degradation and the hostile XML/path matrix also pass.
- Version metadata was raised to `0.1.0`. The first binary-matched rerun correctly refused bridge authority because the pre-gate manifest still pinned add-on `0.0.0-b`; only that compatibility pin is being advanced before repeating the full gate, without promoting capability evidence early.
- The final server/add-on `0.1.0` rerun passed end to end: status p95 2.85 ms, 5,000-node read p95 85.59 ms, GUI event p95 882.00 ms, and reconnect 221.11 ms. Capability evidence was promoted only after this pass.
- Adversarial review added a direct EDT snapshot-slice gate and exposed a 170.39 ms first capture, so that report was rejected. The capture already constructs deterministic `LinkedHashMap` data and sorted connectors; redundant recursive key sorting before hashing is being removed and must requalify.
- Direct deterministic JSON hashing reduced the 5,000-node read p95 to 57.57 ms and maximum measured EDT snapshot slice to 95.49 ms. The complete `0.1.0` live/file/XML/reconnect qualifier passed again with all hard gates.
- Final adversarial review added strict document-root parsing, bounded bridge-response streaming, selection-bound page cursors, route/capability honesty, transition-aware external-file detection, per-map EDT polling slices, and process-RSS journal measurement.
- The first full adversarial rerun rejected a 106.54 ms cold EDT snapshot. Caching repeated Freeplane node proxy lookups reduced the next cold slice to 89.90 ms; that run was still rejected after Freeplane's editor hit a transient null focus owner on edit 11.
- After active Swing focus recovery and shared snapshot-allocation reductions, the final v0.1 gate passed 18/18 checks: status p95 2.59 ms, 5,000-node read p95 45.06 ms, maximum EDT slice 82.47 ms, GUI event p95 945.48 ms, journal RSS delta 212,992 bytes, and reconnect 228.13 ms. All eight hard-gate predicates are true, and only the user Freeplane process remained after cleanup.
- Committed v0.1 as `c48d99c feat(read): qualify v0.1 real-time MCP`, pushed it to the public GitHub branch, and verified the remote SHA plus anonymous commit access.
- Implemented the strict v0.2 apply/history schemas, atomic bridge operations, full virtual-tree validation, connector fingerprint handles, readback verification, destructive confirmation, and private persistent idempotency ledger.
- Rejected a direct `NodeModel` timestamp-compensation approach during adversarial review because it violated the undo-aware-controller rule and created redo history noise. The final design keeps volatile modified times visible but outside the logical canonical hash and skips only bounded hash-invariant metadata history entries.
- The final isolated v0.2 gate passed 27/27 checks: exact eight-tool surface, inherited v0.1 evidence, dry-run zero effect, successful readback, one-step undo/redo equality, 16/16 core failure rollbacks, connector/delete rollback, revision conflict, confirmation, idempotency, privacy, and fixture isolation. Ten 100-operation commits measured p50 137.27 ms and p95/max 199.21 ms.
- Current implementation gate: v0.3 knowledge organization and style. All v0.3 capabilities remain unavailable pending qualification.
- Implemented v0.3 safe organization operations, expanded canonical readback, literal view filtering, native summary hooks, and transactional bookmark actors.
- The final isolated v0.3 gate passed 20/20 checks: exact nine-tool surface, 28/28 failure rollbacks, 35 nodes/33 relations, exact H2 updates, five styles, native summary/clone, literal filter/clear, 16% maximum section-heading overlap, one undo/redo equality, executable-payload denial, and unchanged fixture bytes.
- Adversarial review rejected two intermediate filter reports: the active-map filter lives in Freeplane's selection state, while clear leaves a transparent filter object. The final implementation uses the native filter object for revision signatures and `FilterController.isFilterActive()` for postcondition readback, then reran the entire gate successfully.
- Current implementation gate: v0.4 document lifecycle, export, and protection.
- Implemented revision- and file-hash-guarded document create/template/open/save/save-as/close/revert with deterministic default-template resolution and bound one-time confirmation for overwrites, dirty close, and revert.
- Qualified native map-scope PNG/PDF/SVG/static-HTML export through sibling staging, structural validation, SHA-256 readback, atomic publication, and durable overwrite backup. The gate exposed PDF's asynchronous writer, Batik's fixed SVG DTD/closing syntax, and three competing HTML engines; each was resolved by observed stable behavior or implementation identity rather than order or delay guesses.
- Implemented lexical closed-file ordinary-node `TEXT` writeback with configured-path/open-map guards, hostile XML denial, APFS clone replacement, xattr preservation, and original/candidate/manifest recovery evidence.
- The final v0.4 isolated gate passed 26/26 checks. All lifecycle modes, four exports, confirmation binding, hostile path/XML denial, unknown-XML byte preservation, secure-input blocking, and preservation of the one pre-existing user Freeplane process passed.
- Implemented the v0.5 exact GUI allowlist, signed Swift Accessibility helper, bridge presentation/preview readback, revision/idempotency guards, and the twelfth `freeplane_invoke_action` tool.
- The final v0.5 bilingual isolated gate passed 30/30 checks. English and Simplified Chinese presentation navigation, focus recovery, print-preview cancellation, policy rejection, content immutability, helper signing, and exact preservation of the one pre-existing user Freeplane process all passed.
- Committed v0.5 as `bcde844 feat(gui): qualify v0.5 accessibility actions`, pushed it to the public GitHub branch, and verified the remote SHA plus anonymous commit access.
- Implemented the v1.0 local installer/uninstaller, dedicated-profile launcher, redacted doctor, explicit map/artifact and idempotency recovery, compatibility/security/recovery documentation, SPDX SBOM, and frozen final capability table.
- Frozen the v1.0 execution boundary: explicit temporary install/profile paths, manifest-owned uninstall, read-only redacted diagnostics, no automatic recovery choice, no public listener/upload/telemetry/arbitrary scripts, and no formal release or licensing claim.
- The final v1.0 isolated gate passed 16/16 checks with all nine hard gates true. It verified 947 hash-owned install files, repeated install, zero public listeners, least permissions, graceful and forced-crash restarts, explicit recovery, recoverable uninstall, capability-hash equality, and exact preservation of the pre-existing user Freeplane process.
